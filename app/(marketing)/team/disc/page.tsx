import DiscTeamClient from "./DiscTeamClient";

export const metadata = {
  title: "DISC Profile — Team | Crispy Development",
};

export default function DiscTeamPage() {
  return <DiscTeamClient />;
}
